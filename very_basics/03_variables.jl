##### Real numbers: Float64
x = 3.14
typeof(x)

##### Integers: Int64
y = 22
typeof(y)

##### Boolean: Bool
mortality = true
typeof(mortality)

# Packages, Modules and types are all title case, everything else is lower case

##### Strings: String
my_string = "Start Julia"
typeof(my_string)

# Single quote in Julia is reserved only for char types